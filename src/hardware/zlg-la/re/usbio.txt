/* This file was generated by the Hex-Rays decompiler version 9.3.0.260327.
   Copyright (c) 2007-2025 Hex-Rays <info@hex-rays.com>

   Detected compiler: Visual C++
*/

#include <windows.h>
#include <defs.h>


//-------------------------------------------------------------------------
// Function declarations

int __cdecl sub_10001010(GUID *ClassGuid, _DWORD *a2, char *RequiredSize);
void __cdecl InitializeInterface(struct _GUID *a1); // idb
int __cdecl OpenPort(int a1, char *a2, DWORD dwFlagsAndAttributes);
BOOL __cdecl WritePort(int a1, int a2, LPCVOID lpBuffer, DWORD nNumberOfBytesToWrite, LPDWORD lpNumberOfBytesWritten, DWORD dwMilliseconds);
BOOL __cdecl ReadPort(int a1, int a2, void *lpBuffer, DWORD nNumberOfBytesToRead, LPDWORD lpNumberOfBytesRead, DWORD dwMilliseconds);
// void __cdecl operator delete(void *); idb
// void *__cdecl operator new(unsigned int); idb
// int sprintf(char *const Buffer, const char *const Format, ...);

//-------------------------------------------------------------------------
// Data declarations

// extern DWORD (__stdcall *GetLastError)();
// extern HANDLE (__stdcall *CreateFileA)(LPCSTR lpFileName, DWORD dwDesiredAccess, DWORD dwShareMode, LPSECURITY_ATTRIBUTES lpSecurityAttributes, DWORD dwCreationDisposition, DWORD dwFlagsAndAttributes, HANDLE hTemplateFile);
// extern BOOL (__stdcall *GetOverlappedResult)(HANDLE hFile, LPOVERLAPPED lpOverlapped, LPDWORD lpNumberOfBytesTransferred, BOOL bWait);
// extern BOOL (__stdcall *CloseHandle)(HANDLE hObject);
// extern BOOL (__stdcall *CancelIo)(HANDLE hFile);
// extern DWORD (__stdcall *WaitForSingleObject)(HANDLE hHandle, DWORD dwMilliseconds);
// extern BOOL (__stdcall *WriteFile)(HANDLE hFile, LPCVOID lpBuffer, DWORD nNumberOfBytesToWrite, LPDWORD lpNumberOfBytesWritten, LPOVERLAPPED lpOverlapped);
// extern HANDLE (__stdcall *CreateEventA)(LPSECURITY_ATTRIBUTES lpEventAttributes, BOOL bManualReset, BOOL bInitialState, LPCSTR lpName);
// extern BOOL (__stdcall *ReadFile)(HANDLE hFile, LPVOID lpBuffer, DWORD nNumberOfBytesToRead, LPDWORD lpNumberOfBytesRead, LPOVERLAPPED lpOverlapped);
// extern BOOL (__stdcall *SetupDiEnumDeviceInterfaces)(HDEVINFO DeviceInfoSet, PSP_DEVINFO_DATA DeviceInfoData, const GUID *InterfaceClassGuid, DWORD MemberIndex, PSP_DEVICE_INTERFACE_DATA DeviceInterfaceData);
// extern BOOL (__stdcall *SetupDiGetDeviceInterfaceDetailA)(HDEVINFO DeviceInfoSet, PSP_DEVICE_INTERFACE_DATA DeviceInterfaceData, PSP_DEVICE_INTERFACE_DETAIL_DATA_A DeviceInterfaceDetailData, DWORD DeviceInterfaceDetailDataSize, PDWORD RequiredSize, PSP_DEVINFO_DATA DeviceInfoData);
// extern BOOL (__stdcall *SetupDiDestroyDeviceInfoList)(HDEVINFO DeviceInfoSet);
// extern HDEVINFO (__stdcall *SetupDiGetClassDevsA)(const GUID *ClassGuid, PCSTR Enumerator, HWND hwndParent, DWORD Flags);
GUID ClassGuid = { 0u, 0u, 0u, { 0u, 0u, 0u, 0u, 0u, 0u, 0u, 0u } }; // idb
_DWORD dword_10003030 = 0; // weak
char byte_10003034[2056] = {0}; // weak


//----- (10001010) --------------------------------------------------------
int __cdecl sub_10001010(GUID *ClassGuid, _DWORD *a2, char *RequiredSize)
{
  HDEVINFO ClassDevsA; // ebp
  struct _SP_DEVICE_INTERFACE_DETAIL_DATA_A *v4; // ebx
  signed int v6; // [esp+10h] [ebp-28h]
  char *v7; // [esp+14h] [ebp-24h]
  int v8; // [esp+18h] [ebp-20h]
  struct _SP_DEVICE_INTERFACE_DATA DeviceInterfaceData; // [esp+1Ch] [ebp-1Ch] BYREF

  v8 = 1;
  ClassDevsA = SetupDiGetClassDevsA(ClassGuid, nullptr, nullptr, 0x12u);
  DeviceInterfaceData.cbSize = 28;
  *a2 = 0;
  v6 = 0;
  if ( SetupDiEnumDeviceInterfaces(ClassDevsA, nullptr, ClassGuid, 0, &DeviceInterfaceData) )
  {
    v7 = RequiredSize;
    while ( v6 < 8 )
    {
      if ( !SetupDiGetDeviceInterfaceDetailA(
              ClassDevsA,
              &DeviceInterfaceData,
              nullptr,
              0,
              (PDWORD)&RequiredSize,
              nullptr)
        && GetLastError() != 122
        || (v4 = (struct _SP_DEVICE_INTERFACE_DETAIL_DATA_A *)operator new((unsigned int)RequiredSize),
            v4->cbSize = 5,
            !SetupDiGetDeviceInterfaceDetailA(
               ClassDevsA,
               &DeviceInterfaceData,
               v4,
               (DWORD)RequiredSize,
               (PDWORD)&RequiredSize,
               nullptr)) )
      {
        v8 = 0;
        break;
      }
      strcpy(v7, v4->DevicePath);
      ++*a2;
      operator delete(v4);
      ++v6;
      v7 += 256;
      if ( !SetupDiEnumDeviceInterfaces(ClassDevsA, nullptr, ClassGuid, v6, &DeviceInterfaceData) )
        break;
    }
  }
  SetupDiDestroyDeviceInfoList(ClassDevsA);
  return v8;
}

//----- (10001170) --------------------------------------------------------
void __cdecl InitializeInterface(struct _GUID *a1)
{
  memset(byte_10003034, 0, 0x800u);
  ClassGuid = *a1;
}

//----- (100011B0) --------------------------------------------------------
int __cdecl OpenPort(int a1, char *a2, DWORD dwFlagsAndAttributes)
{
  char *v3; // esi
  CHAR FileName[253]; // [esp+4h] [ebp-100h] BYREF
  __int16 v6; // [esp+101h] [ebp-3h]
  char v7; // [esp+103h] [ebp-1h]

  v3 = &byte_10003034[256 * a1];
  if ( *v3 )
    goto LABEL_6;
  if ( !sub_10001010(&ClassGuid, &dword_10003030, byte_10003034) )
    return -1;
  if ( !*v3 )
    return -1;
LABEL_6:
  memset(FileName, 0, sizeof(FileName));
  v6 = 0;
  v7 = 0;
  strcpy(FileName, v3);
  strcat(FileName, "\\");
  strcat(FileName, a2);
  return (int)CreateFileA(FileName, 0xC0000000, 3u, nullptr, 3u, dwFlagsAndAttributes, nullptr);
}
// 10003030: using guessed type _DWORD dword_10003030;

//----- (100012D0) --------------------------------------------------------
BOOL __cdecl WritePort(
        int a1,
        int a2,
        LPCVOID lpBuffer,
        DWORD nNumberOfBytesToWrite,
        LPDWORD lpNumberOfBytesWritten,
        DWORD dwMilliseconds)
{
  int v6; // eax
  void *v7; // edi
  BOOL OverlappedResult; // esi
  DWORD v9; // eax
  char Buffer[8]; // [esp+Ch] [ebp-1Ch] BYREF
  struct _OVERLAPPED Overlapped; // [esp+14h] [ebp-14h] BYREF

  sprintf(Buffer, "PIPE%02d", 2 * a2 - 1);
  v6 = OpenPort(a1, Buffer, 0x40000000u);
  memset(&Overlapped, 0, sizeof(Overlapped));
  v7 = (void *)v6;
  Overlapped.hEvent = CreateEventA(nullptr, 1, 0, nullptr);
  *lpNumberOfBytesWritten = 0;
  OverlappedResult = 1;
  if ( !WriteFile(v7, lpBuffer, nNumberOfBytesToWrite, lpNumberOfBytesWritten, &Overlapped) )
  {
    if ( GetLastError() == 997 )
    {
      v9 = WaitForSingleObject(Overlapped.hEvent, dwMilliseconds);
      if ( !v9 )
      {
        OverlappedResult = GetOverlappedResult(v7, &Overlapped, lpNumberOfBytesWritten, 0);
        goto LABEL_7;
      }
      if ( v9 == 258 )
        CancelIo(v7);
    }
    OverlappedResult = 0;
  }
LABEL_7:
  CloseHandle(Overlapped.hEvent);
  CloseHandle(v7);
  return OverlappedResult;
}

//----- (100013C0) --------------------------------------------------------
BOOL __cdecl ReadPort(
        int a1,
        int a2,
        void *lpBuffer,
        DWORD nNumberOfBytesToRead,
        LPDWORD lpNumberOfBytesRead,
        DWORD dwMilliseconds)
{
  int v6; // eax
  void *v7; // edi
  BOOL OverlappedResult; // esi
  DWORD v9; // eax
  char Buffer[8]; // [esp+10h] [ebp-1Ch] BYREF
  struct _OVERLAPPED Overlapped; // [esp+18h] [ebp-14h] BYREF

  sprintf(Buffer, "PIPE%02d", 2 * a2 - 2);
  v6 = OpenPort(a1, Buffer, 0x40000000u);
  memset(&Overlapped, 0, sizeof(Overlapped));
  v7 = (void *)v6;
  Overlapped.hEvent = CreateEventA(nullptr, 1, 0, nullptr);
  *lpNumberOfBytesRead = 0;
  OverlappedResult = 1;
  if ( !ReadFile(v7, lpBuffer, nNumberOfBytesToRead, lpNumberOfBytesRead, &Overlapped) )
  {
    if ( GetLastError() == 997 )
    {
      v9 = WaitForSingleObject(Overlapped.hEvent, dwMilliseconds);
      if ( !v9 )
      {
        OverlappedResult = GetOverlappedResult(v7, &Overlapped, lpNumberOfBytesRead, 0);
        GetLastError();
        goto LABEL_7;
      }
      if ( v9 == 258 )
        CancelIo(v7);
    }
    OverlappedResult = 0;
  }
LABEL_7:
  CloseHandle(Overlapped.hEvent);
  CloseHandle(v7);
  return OverlappedResult;
}

// nfuncs=12 queued=5 decompiled=5 lumina nreq=0 worse=0 better=0
// ALL OK, 5 function(s) have been successfully decompiled
